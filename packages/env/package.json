{
  "name": "@cursor-usage/env",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts",
    "dev": "tsup src/index.ts --format cjs,esm --dts --watch",
    "clean": "rimraf dist .turbo",
    "lint": "eslint ."
  },
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@cursor-usage/config": "workspace:*",
    "dotenv": "^16.4.5",
    "rimraf": "^5.0.5",
    "tsup": "^8.0.2"
  }
}